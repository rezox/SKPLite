unit ModulSrc;

interface

uses
  System.SysUtils, System.Classes, Data.DB, DBAccess, Uni, MemDS, UniProvider,
  SQLiteUniProvider, ZAbstractTable, ZDataset, ZAbstractRODataset,
  ZAbstractDataset, ZAbstractConnection, ZConnection;

type
  TDM = class(TDataModule)
    MyConn: TZConnection;
    Qexec: TZQuery;
    tblpejabat: TZTable;
    Dspejabat: TDataSource;
    tbljabatan: TZTable;
    DsJabatan: TDataSource;
    tblsatker: TZTable;
    Dssatker: TDataSource;
  private
    { Private declarations }
  public
    { Public declarations }
    function Cekidada(Qstr:TZQuery;idcari:string):boolean;
  end;

var
  DM: TDM;

implementation

{%CLASSGROUP 'System.Classes.TPersistent'}

{$R *.dfm}

Function TDm.Cekidada(Qstr;TZQuery; idcari: string):boolean;
begin
  if idcari = Qstr.fieldByName( then
      then

end;

end.
